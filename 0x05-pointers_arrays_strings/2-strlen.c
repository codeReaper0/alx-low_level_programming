#include "main.h"

/**
 * _strlen - returns the length of a string
 * @n: checks the value
 * Tella Boluwatife - Author
 * Return: Always 0.
 */

int _strlen(char *s)
{
	char *s[50] = "Program";
	printf("%zu", _strlen(*s));
}
